# career-engine

Repo entrypoint.

Main artifact: `skills/career-engine/SKILL.md`

Rules:
- default to markdown artifacts
- do not assume Node/Python/TeX/etc. already exist
- do not execute code unless necessary
- when execution is necessary, prefer `uv run` or `uvx`
- optimize for understanding real work and social reality, not application throughput
